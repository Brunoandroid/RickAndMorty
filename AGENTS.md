# Instructions for AI

Never read or process files that:
1. Are listed in `.gitignore`
2. Contain sensitive data, secrets, passwords, or credentials

Always respect the `.gitignore` rules and do not access files that have been explicitly ignored by versioning.